# This script actually runs the RAT jobs found in the jobqueue file.
# It writes out the stdout output of RAT to batch.log.

#! /bin/bash
DIR=$(dirname $0)
LOG=batch.log

source ~/rat_env.sh

tail -f -c +1 $DIR/jobqueue.txt | parallel -j4 --retries 4 --progress >> $LOG

