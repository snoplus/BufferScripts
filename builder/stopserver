#! /bin/bash
FILE=filereceived

while true
do

    if [ -f $FILE ]
        then
            echo File received
            kill $(pgrep tail)
            rm $FILE
        else
            echo waiting
            sleep 1
    fi

done
